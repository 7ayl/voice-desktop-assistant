# 使用说明 - Usage Guide

## 目录
1. [快速开始](#快速开始)
2. [命令示例](#命令示例)
3. [进阶用法](#进阶用法)
4. [常见问题](#常见问题)
5. [API 参考](#api-参考)

---

## 快速开始

### 安装和启动

```bash
# 1. 安装依赖
pip install -r requirements.txt

# 2. 配置环境
cp .env .env
# 编辑 .env 文件（如需使用云服务）

# 3. 启动应用
python app.py

# 4. 打开浏览器
# 访问 http://localhost:5000
```

### 基本操作流程

```
1. 点击 "开始录音" 按钮
   ↓
2. 清晰地说出您的命令
   ↓
3. 点击 "停止录音" 按钮
   ↓
4. 系统自动识别和执行
   ↓
5. 获得操作结果和语音反馈
```

---

## 命令示例

### 1. 创建文件夹

#### 示例 1.1
```
用户说: "帮我在D盘创建一个名称为'我的作业'的文件夹"
系统识别: 创建文件夹
执行结果: ✓ 成功创建 D:\我的作业
```

#### 示例 1.2
```
用户说: "在E盘创建'项目资料'文件夹"
系统识别: 创建文件夹
执行结果: ✓ 成功创建 E:\项目资料
```

#### 示例 1.3
```
用户说: "新建C盘test文件夹"
系统识别: 创建文件夹
执行结果: ✓ 成功创建 C:\test
```

### 2. 删除文件和文件夹

#### 示例 2.1
```
用户说: "帮我删除D盘里名称为'oldfile'的文件夹"
系统识别: 删除文件夹
执行结果: ✓ 成功删除 D:\oldfile
```

#### 示例 2.2
```
用户说: "在E盘删除'temp'文件"
系统识别: 删除文件
执行结果: ✓ 成功删除 E:\temp
```

### 3. 复制文件和文件夹

#### 示例 3.1
```
用户说: "帮我把D盘的'我的考试'word文档复制到E盘"
系统识别: 复制文件
源位置: D:\我的考试
目标位置: E:\
执行结果: ✓ 成功复制 D:\我的考试 -> E:\我的考试
```

#### 示例 3.2
```
用户说: "复制D盘的project文件夹到F盘"
系统识别: 复制文件夹
执行结果: ✓ 成功复制 D:\project -> F:\project
```

### 4. 移动文件和文件夹

#### 示例 4.1
```
用户说: "帮我把D盘里的'我的文档'移到E盘"
系统识别: 移动文件夹
执行结果: ✓ 成功移动 D:\我的文档 -> E:\我的文档
```

#### 示例 4.2
```
用户说: "把E盘的'report.docx'移到D盘"
系统识别: 移动文件
执行结果: ✓ 成功移动 E:\report.docx -> D:\report.docx
```

### 5. 搜索文件和文件夹

#### 示例 5.1
```
用户说: "帮我在E盘查找是否有名称包含'测试'的文件"
系统识别: 搜索
搜索范围: E:\
搜索关键词: 测试
执行结果: 找到 3 个包含'测试'的项目:
  - E:\测试报告.docx
  - E:\测试数据.xlsx
  - E:\测试文件夹\
```

#### 示例 5.2
```
用户说: "在D盘搜索'2024'相关的文件"
系统识别: 搜索
执行结果: 找到 5 个包含'2024'的项目
```

### 6. 重命名���件和文件夹

#### 示例 6.1
```
用户说: "帮我把D盘的'oldname'改成'newname'"
系统识别: 重命名文件
执行结果: ✓ 成功重命名 D:\oldname -> D:\newname
```

#### 示例 6.2
```
用户说: "把E盘的'项目'文件夹改成'完成的项目'"
系统识别: 重命名文件夹
执行结果: ✓ 成功重命名 E:\项目 -> E:\完成的项目
```

---

## 进阶用法

### 1. 使用文本输入

如果语音识别不准确，可以直接在文本框输入命令：

```
1. 在 "或直接输入命令" 的文本框中输入您的命令
2. 按 Ctrl+Enter 或点击 "执行命令" 按钮
3. 系统会处理您的命令
```

### 2. 查看操作历史

```
- 所有操作都会记录在 "操作历史" 部分
- 可以查看最近 50 条操作
- 点击 "清除历史" 按钮可以清空历史记录
```

### 3. 系统状态检查

```
页面顶部显示系统状态：
- 系统状态：正常/异常
- ASR 引擎：就绪/不可用
- TTS 引擎：就绪/不可用
```

### 4. 调整识别语言

编辑 `.env` 文件：

```env
# 中文（默认）
ASR_LANGUAGE=zh_CN
TTS_LANGUAGE=zh-CN

# 英文
ASR_LANGUAGE=en_US
TTS_LANGUAGE=en-US
```

### 5. 更换 ASR/TTS 服��

#### 使用百度云 ASR

```env
ASR_SERVICE=baidu
ASR_API_KEY=your_baidu_api_key
BAIDU_SECRET_KEY=your_secret_key
```

#### 使用讯飞 ASR

```env
ASR_SERVICE=iflytek
ASR_API_KEY=your_iflytek_api_key
```

---

## 常见问题

### Q1: 语音识别总是出错

**A**: 
- 确保说话清晰，避免背景噪音
- 使用高质量麦克风
- 检查网络连接（使用云服务时）
- 尝试更换 ASR 服务

### Q2: 文件操作失败了

**A**:
- 检查目标路径是否存在
- 确保有足够的文件访问权限
- 查看日志: `logs/app.log`
- 避免对系统目录进行操作

### Q3: 如何查看详细日志

**A**:
```bash
# 实时查看日志
tail -f logs/app.log

# 搜索特定错误
grep "ERROR" logs/app.log
```

### Q4: 命令没有被正确理解

**A**:
- 使用更清晰的表述
- 明确指定驱动器字母（D、E等）
- 使用文本输入而不是语音
- 检查命令解析的置信度

### Q5: 支持哪些驱动器

**A**: 
默认支持 C:, D:, E:, F: 驱动器

编辑 `.env` 修改：
```env
DEFAULT_DRIVES=C:,D:,E:,F:,G:
```

---

## API 参考

### 系统健康检查 - GET /api/health

```bash
curl http://localhost:5000/api/health
```

响应:
```json
{
  "status": "healthy",
  "timestamp": "2024-06-11T12:00:00.000000",
  "engines": {
    "asr": "ready",
    "tts": "ready",
    "nlu": "ready",
    "file_operator": "ready"
  }
}
```

### 获取操作历史 - GET /api/history

```bash
# 获取最近 20 条
curl "http://localhost:5000/api/history?limit=20&offset=0"
```

响应:
```json
{
  "success": true,
  "total": 42,
  "records": [
    {
      "timestamp": "2024-06-11T12:00:00",
      "command": "帮我在D盘创建一个名称为'test'的文件夹",
      "operation": "create",
      "success": true,
      "message": "成功创建文件夹: test"
    }
  ]
}
```

---

## 命令模式识别

系统支持以下命令模式（使用正则表达式匹配）：

### 创建
```
关键词: 创建, 新建, 建立
模式: [驱动器][操作][文件名][文件类型]
例子: "在D盘创建'test'文件夹"
```

### 删除
```
关键词: 删除, 移除, 清除
模式: [操作][驱动器][文件名]
例子: "删除D盘里的'test'"
```

### 复制
```
关键词: 复制, 拷贝, 备份
模式: [操作][源][文件名][目标驱动器]
例子: "把D盘的'file'复制到E盘"
```

### 移动
```
关键词: 移动, 移到, 移入
模式: [操作][源][文件名][目标驱动器]
例子: "把D盘的'file'移到E盘"
```

### 搜索
```
关键词: 查找, 搜索, 寻找
模式: [操作][驱动器][关键词]
例子: "在E盘查找'test'"
```

### 重命名
```
关键词: 改名, 重命名, 改成
模式: [操作][源][旧名][新名]
例子: "把D盘的'old'改成'new'"
```

---

## 故障排除

### 检查清单

- [ ] Flask 服务器正在运行（http://localhost:5000 可访问）
- [ ] 浏览器允许麦克风访问
- [ ] 网络连接正常
- [ ] ASR/TTS 服务可用
- [ ] 目标路径存在
- [ ] 有足够的文件权限

### 调试模式

启用调试模式查看详细日志：

```env
FLASK_DEBUG=True
LOG_LEVEL=DEBUG
```

然后查看 `logs/app.log` 获取详细信息。

---

**最后更新**: 2024年6月11日
